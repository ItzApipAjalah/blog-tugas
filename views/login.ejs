<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Login</title>

  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Lucide Icons -->
  <script src="https://unpkg.com/lucide@latest"></script>

  <!-- Font -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Inter', sans-serif;
    }

    /* Simple fade-in animation */
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .fade-in {
      animation: fadeIn 0.7s ease-out;
    }
  </style>
</head>
<body class="min-h-screen flex items-center justify-center bg-gradient-to-br from-gray-50 via-white to-gray-100 text-gray-800">

  <div class="w-full max-w-md bg-white rounded-2xl shadow-2xl p-8 border border-gray-200 fade-in transform transition-all duration-300 hover:scale-[1.02]">
    <div class="text-center mb-8">
      <div class="flex justify-center mb-3">
        <i data-lucide="lock" class="w-12 h-12 text-indigo-500 animate-pulse"></i>
      </div>
      <h1 class="text-3xl font-semibold text-indigo-600 tracking-wide">Admin Login</h1>
      <p class="text-gray-500 mt-1 text-sm">Secure access to your dashboard</p>
    </div>

    <form action="/login" method="POST" class="space-y-5">
      <div class="fade-in" style="animation-delay: 0.1s; animation-fill-mode: both;">
        <label class="block mb-2 text-sm font-medium text-gray-700">Username</label>
        <div class="relative">
          <input 
            type="text" 
            name="username" 
            required
            class="w-full px-4 py-3 pl-10 bg-gray-50 border border-gray-300 rounded-xl text-gray-800 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-400 focus:border-indigo-400 transition"
            placeholder="Enter your username"
          />
          <i data-lucide="user" class="absolute left-3 top-3.5 w-5 h-5 text-gray-400"></i>
        </div>
      </div>

      <div class="fade-in" style="animation-delay: 0.2s; animation-fill-mode: both;">
        <label class="block mb-2 text-sm font-medium text-gray-700">Password</label>
        <div class="relative">
          <input 
            type="password" 
            name="password" 
            required
            class="w-full px-4 py-3 pl-10 bg-gray-50 border border-gray-300 rounded-xl text-gray-800 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-400 focus:border-indigo-400 transition"
            placeholder="Enter your password"
          />
          <i data-lucide="key-round" class="absolute left-3 top-3.5 w-5 h-5 text-gray-400"></i>
        </div>
      </div>

      <button 
        type="submit"
        class="w-full bg-indigo-600 hover:bg-indigo-700 active:scale-[0.98] transition transform duration-150 text-white font-semibold py-3 rounded-xl shadow-md shadow-indigo-300/40"
      >
        Login
      </button>
    </form>

    <p class="text-center text-gray-400 text-sm mt-6">© 2025 Admin Panel</p>
  </div>

  <script>
    lucide.createIcons();
  </script>
</body>
</html>
